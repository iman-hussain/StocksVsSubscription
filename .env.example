# Backend Environment Variables

# Node environment (development or production)
NODE_ENV=production

# Redis URL for caching (optional, but recommended for production)
# If not set, the backend will work without caching
# For local development: redis://localhost:6379
# For production: redis://your-redis-host:6379
REDIS_URL=redis://localhost:6379

# Port for the backend server (default: 3000)
PORT=3000

# Docker Compose Build Variables
# This is used when building the Docker image to configure the frontend API URL
# For local development: http://localhost:3000
# For production: https://api.svs.imanhussain.com
VITE_API_URL=http://localhost:3000